package com.scm.enums;

public enum MessageTypes {
  
  // by degault enums written in uppercase  
  // here we will use the lowercase enums to append in thymeleaf classes directly

  red, green, blue, yellow
}
